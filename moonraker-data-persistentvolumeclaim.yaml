apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: moonraker-data
  name: moonraker-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
