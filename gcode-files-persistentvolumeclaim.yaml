apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: gcode-files
  name: gcode-files
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
